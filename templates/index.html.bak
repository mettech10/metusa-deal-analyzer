<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Metusa Deal Analyzer | Property Investment Analysis</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #1B1F3B 0%, #2d3561 100%);
            min-height: 100vh;
            color: #333;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 40px 20px;
        }
        
        .header {
            text-align: center;
            margin-bottom: 40px;
            color: white;
        }
        
        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
        }
        
        .header p {
            font-size: 1.1rem;
            opacity: 0.9;
        }
        
        .form-card {
            background: white;
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
        }
        
        .form-section {
            margin-bottom: 30px;
        }
        
        .form-section h3 {
            color: #1B1F3B;
            margin-bottom: 20px;
            font-size: 1.2rem;
            border-bottom: 2px solid #D4AF37;
            padding-bottom: 10px;
        }
        
        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .form-group {
            display: flex;
            flex-direction: column;
        }
        
        .form-group.full-width {
            grid-column: 1 / -1;
        }
        
        label {
            font-weight: 600;
            margin-bottom: 8px;
            color: #555;
            font-size: 0.9rem;
        }
        
        input, select {
            padding: 14px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s;
        }
        
        input:focus, select:focus {
            outline: none;
            border-color: #D4AF37;
        }
        
        .price-input {
            position: relative;
        }
        
        .price-input::before {
            content: "£";
            position: absolute;
            left: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: #666;
            font-weight: 600;
        }
        
        .price-input input {
            padding-left: 32px;
        }
        
        .percent-input {
            position: relative;
        }
        
        .percent-input::after {
            content: "%";
            position: absolute;
            right: 16px;
            top: 50%;
            transform: translateY(-50%);
            color: #666;
            font-weight: 600;
        }
        
        .percent-input input {
            padding-right: 32px;
        }
        
        .submit-btn {
            background: linear-gradient(135deg, #D4AF37 0%, #b8962e 100%);
            color: #1B1F3B;
            border: none;
            padding: 18px 40px;
            font-size: 1.1rem;
            font-weight: bold;
            border-radius: 8px;
            cursor: pointer;
            width: 100%;
            transition: transform 0.2s, box-shadow 0.2s;
        }
        
        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(212, 175, 55, 0.4);
        }
        
        .features {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-top: 40px;
            text-align: center;
        }
        
        .feature {
            color: white;
            padding: 20px;
        }
        
        .feature-icon {
            font-size: 2rem;
            margin-bottom: 10px;
        }
        
        .feature h4 {
            font-size: 1rem;
            margin-bottom: 5px;
        }
        
        .feature p {
            font-size: 0.85rem;
            opacity: 0.8;
        }
        
        @media (max-width: 600px) {
            .form-row {
                grid-template-columns: 1fr;
            }
            
            .features {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 1.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🏠 Metusa Deal Analyzer</h1>
            <p>Professional property investment analysis in 60 seconds</p>
        </div>
        
        <div class="form-card">
            <form id="dealForm" action="/analyze" method="POST">
                
                <!-- Property Details -->
                <div class="form-section">
                    <h3>📍 Property Details</h3>
                    <div class="form-row">
                        <div class="form-group full-width">
                            <label for="address">Property Address</label>
                            <input type="text" id="address" name="address" placeholder="e.g., 42 Oakfield Avenue, Manchester" required>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label for="postcode">Postcode</label>
                            <input type="text" id="postcode" name="postcode" placeholder="M14 6LT" required>
                        </div>
                        <div class="form-group">
                            <label for="dealType">Deal Type</label>
                            <select id="dealType" name="dealType" required>
                                <option value="BTL">Buy-to-Let (BTL)</option>
                                <option value="BRR">Buy Refurb Refinance (BRR)</option>
                                <option value="HMO">HMO (House of Multiple Occupation)</option>
                                <option value="FLIP">Flip/Renovation</option>
                            </select>
                        </div>
                    </div>
                </div>
                
                <!-- Financial Details -->
                <div class="form-section">
                    <h3>💰 Financial Details</h3>
                    <div class="form-row">
                        <div class="form-group price-input">
                            <label for="purchasePrice">Purchase Price</label>
                            <input type="number" id="purchasePrice" name="purchasePrice" placeholder="185000" required>
                        </div>
                        <div class="form-group price-input">
                            <label for="monthlyRent">Monthly Rent</label>
                            <input type="number" id="monthlyRent" name="monthlyRent" placeholder="950" required>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group percent-input">
                            <label for="deposit">Deposit (%)</label>
                            <input type="number" id="deposit" name="deposit" value="25" min="0" max="100" required>
                        </div>
                        <div class="form-group percent-input">
                            <label for="interestRate">Interest Rate (%)</label>
                            <input type="number" id="interestRate" name="interestRate" value="5.5" step="0.1" required>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group price-input">
                            <label for="refurbCosts">Refurbishment Costs (if BRR)</label>
                            <input type="number" id="refurbCosts" name="refurbCosts" placeholder="12000">
                        </div>
                        <div class="form-group price-input">
                            <label for="arv">After Repair Value (if BRR)</label>
                            <input type="number" id="arv" name="arv" placeholder="155000">
                        </div>
                    </div>
                </div>
                
                <!-- Contact -->
                <div class="form-section">
                    <h3>📧 Get Your Report</h3>
                    <div class="form-row">
                        <div class="form-group">
                            <label for="email">Your Email</label>
                            <input type="email" id="email" name="email" placeholder="you@email.com" required>
                        </div>
                        <div class="form-group">
                            <label for="name">Your Name</label>
                            <input type="text" id="name" name="name" placeholder="John Smith" required>
                        </div>
                    </div>
                </div>
                
                <button type="submit" class="submit-btn">
                    📊 Generate Deal Analysis Report
                </button>
            </form>
        </div>
        
        <div class="features">
            <div class="feature">
                <div class="feature-icon">⚡</div>
                <h4>Instant Analysis</h4>
                <p>Get your report in under 60 seconds</p>
            </div>
            <div class="feature">
                <div class="feature-icon">📈</div>
                <h4>Professional PDF</h4>
                <p>Investor-ready reports with full metrics</p>
            </div>
            <div class="feature">
                <div class="feature-icon">🎯</div>
                <h4>Expert Verdict</h4>
                <p>PROCEED, REVIEW, or AVOID recommendation</p>
            </div>
        </div>
    </div>
    
    <script>
        document.getElementById('dealForm').addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Show loading state
            const btn = document.querySelector('.submit-btn');
            btn.innerHTML = '⏳ Analyzing your deal...';
            btn.disabled = true;
            
            // Collect form data
            const formData = new FormData(this);
            const data = Object.fromEntries(formData);
            
            // Here you would send to your backend
            console.log('Deal data:', data);
            
            // For now, show alert (replace with actual API call)
            setTimeout(() => {
                alert('✅ Deal analysis complete! Your PDF report has been emailed to you.');
                btn.innerHTML = '📊 Generate Deal Analysis Report';
                btn.disabled = false;
            }, 2000);
        });
    </script>
</body>
</html>
